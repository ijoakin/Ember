# Ember

A C++ project built with CMake using MinGW on Windows.

## Requirements

- CMake 3.20 or higher
- MinGW Makefiles
- C++23 compatible compiler

## Build Instructions

### 1. Configure the project
```bash
cd build
cmake .. -G "MinGW Makefiles"